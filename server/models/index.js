const User = require('./User');
const Animal = require('./Animal');
const Category = require('./Category');
const Donation = require('./Donation');

module.exports = { User, Animal, Category, Donation };
